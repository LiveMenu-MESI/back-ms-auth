# Postgres (compose)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
KEYSTORE_DNS=
DB_PORT=

# Keycloak DB (must match init + userlist: keycloak/keycloak123)
KC_DB_URL=
KC_DB_USERNAME=
KC_DB_PASSWORD=

# Keycloak admin console
KEYCLOAK_ADMIN=
KEYCLOAK_ADMIN_PASSWORD=
#KC_HOSTNAME=
KC_HOSTNAME_STRICT=
KC_HOSTNAME_PORT=
KC_HTTP_ENABLED=

# App DB user (init script + pgbouncer; same user as Quarkus uses)
DB_PASSWORD=

# Client secrets (realm import + Quarkus). Must match realm client config.
OIDC_CLIENT_SECRET=
KEYCLOAK_ADMIN_CLIENT_SECRET=

DB_URL=
DB_USER=
DB_PASSWORD=

KEYCLOAK_URL=

# HTTPS (keystore: compose/config/keystore.p12)
HTTPS_ENABLED=
HTTPS_KEYSTORE_FILE=
HTTPS_KEYSTORE_PASSWORD=
HTTPS_INSECURE_REQUESTS=
GCP_STORAGE_BUCKET_NAME=l